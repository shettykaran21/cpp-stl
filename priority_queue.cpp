/* ************************** Priority Queue ****************************** */
#include "priority_queue.h"

#include <bits/stdc++.h>

using namespace std;

void PriorityQueue::priorityQueue() {
  /* Member functions
   *
   * Capacity
   * p.size(), p.empty()
   *
   * Element Access
   * p.top()
   *
   * Modifiers
   * p.push(), p.pop(), p.emplace(), p.swap()
   *
   */
}