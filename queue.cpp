/* ************************** Queue ****************************** */
#include "queue.h"

#include <bits/stdc++.h>

using namespace std;

void Queue::que() {
  /* Member functions
   *
   * Capacity
   * q.size(), q.empty()
   *
   * Element Access
   * q.front(), q.back()
   *
   * Modifiers
   * q.push(), q.pop(), q.emplace(), q.swap()
   *
   */
}
