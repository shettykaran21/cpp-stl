/* ************************** Deque ****************************** */
#include "deque.h"

#include <bits/stdc++.h>

using namespace std;

void Deque::deq() {
  /* Member functions
   *
   * Capacity
   * d.size(), d.max_size(), d.resize(), d.shrink_to_fit(), d.empty()
   *
   * Element Access
   * [], d.at(), d.front(), d.back()
   *
   * Modifiers
   * d.assign(), d.insert(), d.push_back(), d.push_front(), d.pop_back(),
   * d.pop_front(), d.emplace(), d.emplace_back(), d.emplace_front(), d.erase(),
   * d.swap(), d.clear()
   *
   * Others
   * d.get_allocator()
   */

  // ------------------------ Constructors -----------------------//

  // --------------------- Iterators ---------------------- //
  // d.begin(), d.end(), d.rbegin(), d.rend(), d.cbegin(), d.cend(),
  // d.crbegin(), d.crend()

  // ------------------------ Capacity -----------------------//
  // d.size(), d.max_size(), d.resize(), d.shrink_to_fit(), d.empty()

  // ------------------------ Element access ---------------------//
  // [], v.at(), v.front(), v.back()

  // --------------------- Modifiers ------------------------- //
  // d.assign(), d.insert(), d.push_back(), d.push_front(), d.pop_back(),
  // d.pop_front(), d.emplace(), d.emplace_back(), d.emplace_front(), d.erase(),
  // d.swap(), d.clear()

  // -------------------------- Others ------------------------- //
}