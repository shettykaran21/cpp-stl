/* ************************** Stack ****************************** */
#include "stack.h"

#include <bits/stdc++.h>

using namespace std;

void Stack::st() {
  /* Member functions
   *
   * Capacity
   * s.size(), s.empty()
   *
   * Element Access
   * s.top()
   *
   * Modifiers
   * s.push(), s.pop(), s.emplace(), s.swap()
   *
   */
}
